export interface DashboardInterface {
    cmp_uuid: string | null,
    companiesCount: string | null,
    usersCount: string | null,
    itemsCount: string | null,
    modelsItemsCount: string | null,    
    worksCount: string | null,
    customersCount: Date | null
}